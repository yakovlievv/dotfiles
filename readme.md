## installation list:

- tmux
- neovim
- bat
- wget
- cargo
- npm

### Using bat:

- First of all you need to have _wget_ installed
- Secondly after you stow the directory run:

```zsh
bat cache --build
```
